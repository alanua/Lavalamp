# Firmware source

This directory is for firmware source and build configuration for the Lavalamp project.

Current target:
- WLED-based firmware
- ESP32 first
- ESP8266 where practical
- cylindrical 16x16 LED matrix

Do not commit build output here. Firmware binaries belong in GitHub Releases or CI artifacts with sha256 and source commit metadata.
